interface:
  display_name: "check a vendor invoice"
  short_description: "Run this finance workflow with verified evidence"
  default_prompt: "Use $check-a-vendor-invoice to complete and verify this finance task."
