interface:
  display_name: "verify link prefetching"
  short_description: "Run this web workflow with verified evidence"
  default_prompt: "Use $verify-link-prefetching to complete and verify this web task."
