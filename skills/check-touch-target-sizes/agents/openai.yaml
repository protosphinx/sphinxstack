interface:
  display_name: "check touch target sizes"
  short_description: "Run this design workflow with verified evidence"
  default_prompt: "Use $check-touch-target-sizes to complete and verify this design task."
