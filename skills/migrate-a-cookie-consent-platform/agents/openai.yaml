interface:
  display_name: "migrate a cookie consent platform"
  short_description: "Run this web workflow with verified evidence"
  default_prompt: "Use $migrate-a-cookie-consent-platform to complete and verify this web task."
