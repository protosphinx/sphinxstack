interface:
  display_name: "migrate a global domain"
  short_description: "Run this web workflow with verified evidence"
  default_prompt: "Use $migrate-a-global-domain to complete and verify this web task."
